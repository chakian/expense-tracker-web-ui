configuration: Release
cache:
  - packages -> **\packages.config

before_build:
- cmd: >-
    nuget restore ExpenseTracker/ExpenseTracker.sln

build:
  project: ExpenseTracker/ExpenseTracker.sln
  verbosity: minimal

after_build:
  - appveyor PushArtifact ExpenseTracker\ExpenseTracker.WebUI\**\*.* -FileName *.*



deploy:
 provider: Environment
 name: Harcama-Test
 on:
   branch: develop-no
